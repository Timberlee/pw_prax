# pw_prax
Practicing some Playwright

Quick and dirty way to run the site:
npx http-server ./src -p 3000 

Run just the tests against the practice site:
npx playwright test tests/local_page.spec.ts

For ease of use put styles.css and scripts.js in src alongside index.html